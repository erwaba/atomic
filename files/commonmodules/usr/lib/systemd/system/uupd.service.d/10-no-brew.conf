# drop-in that replaces ExecStart with flag to disable brew updates (brew tree will be deleted)
# empty ExecStart= line is needed to clear inherited value
[Service]
ExecStart=
ExecStart=/usr/bin/uupd --disable-module-brew
